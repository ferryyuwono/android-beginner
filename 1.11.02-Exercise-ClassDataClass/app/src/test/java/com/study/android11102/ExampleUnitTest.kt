package com.study.android11102

import org.junit.Test

import org.junit.Assert.*

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
class ExampleUnitTest {
    //TODO: Create data class Person which has firstName, lastName, age, address, city, phoneNumber

    @Test
    fun initializeAndPrintDataClass() {
        //TODO: Create instance of data class using name argument parameter
        //val person1 = ..."Budi", "Arif", 25, "Jalan Sudirman No 1", "DKI Jakarta", "081234567890"

        //TODO: Print data class
        println("Person1: ...")

        assertEquals(4, 2 + 2)
    }

    @Test
    fun createCatClass() {
        //TODO: Create instance of `Cat` class using `Companion Object`  inside it as parameter
        //val cat1 = Cat(...) //ACTION_HUNGRY
        //val cat2 = Cat(...) //ACTION_SLEEPING

        //TODO: Call `printAction()` function from cat1 and cat2


        assertEquals(4, 2 + 2)
    }
}
